# Senate LDA API key (register at https://lda.senate.gov/api/register/)
# Without a key, requests are rate-limited more aggressively.
LDA_API_KEY=your_api_key_here

# Database path (defaults to lda_filings.db in the project root)
LDA_DB_PATH=lda_filings.db
